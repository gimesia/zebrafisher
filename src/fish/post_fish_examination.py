import numpy as np
import cv2

from src.models import InputImage


def post_fish_exam(gray_img: np.ndarray) -> np.ndarray:
    img = gray_img.copy()
    return img


def post_fish_examination(input_img: InputImage) -> InputImage:
    return input_img
